{
  "name": "samplebook-root",
  "version": "1.0.0",
  "description": "A minimal Storybook clone for framework-agnostic component development",
  "private": true,
  "packageManager": "pnpm@10.12.3",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter samplebook dev",
    "example": "pnpm --filter samplebook cli ./packages/example-project"
  }
}
